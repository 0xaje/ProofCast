# Mineral Signal Palette Validation

## Browser observations

The refreshed Signal Room was inspected in the running application on 2026-08-26.

| Route | Observed visual system | Contrast and hierarchy result |
|---|---|---|
| Command Center (`/`) | Plum orientation rail with proof seal, workspace links, and connection module; lilac mineral canvas; chalk metric surfaces; oxide status/action treatment. | The dark rail protects navigation contrast, while the page heading and operational labels remain clearly readable on the lighter field. |
| Market Decision (`/market`) | The same plum rail, lilac canvas, headline hierarchy, execution-status chips, and chalk analysis/book panels remain in place. | The selected navigation state, warning boundary, and primary market surface remain distinguishable without returning to a dark-blue canvas. |
| Proof Profile (`/proof`) | The same rail and canvas frame the calibration cards, Decision Receipt panel, and empty receipt ledger. | Headings, muted explanatory text, status chips, and the empty-state boundary remain readable and separated through chalk, lilac, plum, oxide, aubergine, and ochre treatments. |

The design retains the rail-first Signal Room structure, but its visual identity now depends on a distinctive plum/lilac/oxide mineral palette rather than generic graphite-and-blue fintech styling.

## Loaded-state evidence

After the DreamDEX snapshot resolved on the Market Decision route, the browser displayed the `LIVE` rail connection state, `EXECUTION DISABLED` and `LIVE` status chips, the selected Event Contract panel, on-chain `TRADING` indicator, probability-comparison instrument, YES order-book rows, and the local forecast boundary. The primary market panel and order-book surface remained Chalk against the Lilac Mineral canvas; live/provenance cues used Oxide; model and local forecast cues retained Aubergine; cautionary order-book values retained Ochre. These elements were visually separable without a dark-blue page foundation.
